import { KeycloakConfig } from 'keycloak-js';

const keycloakConfig: KeycloakConfig = {
  url: 'https://sandbox-id.kernotec.com/',
  realm: 'kernobi',
  clientId: 'kbi-contabilidad-ui',
};

export default keycloakConfig;
